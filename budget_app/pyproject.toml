[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "budget_app"
version = "1.0.0"
description = "The budget manager written in Python"
requires-python = ">=3.14.7"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1"
]

[tool.pytest.ini_options]
testpaths = ["test"]